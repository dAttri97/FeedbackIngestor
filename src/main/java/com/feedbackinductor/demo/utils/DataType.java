package com.feedbackinductor.demo.utils;

/**
 * Enum to represent types of records we are using for the current solution.
 * Values can be edited to represent new types.
 */
public enum DataType {
    TWEET,
    POST,
    REVIEW;

}
